{% extends 'base.html.j2' %}

{%block css %}
<link rel="stylesheet" href="{{url_for('static',filename='css/poke.css')}}">
{% endblock %}
{% block title %}
Hi Pokemon
{% endblock %}

{% block content %}
    <h1>Pokemon</h1>

    <form method="POST" action="/poke">  
    <div class="mb-3">
        <label for="name" class="form-label">Pokemon Name</label>
        <input type="text" name="name" class="form-control" id="name">
    </div>

    <button type="submit" class="btn btn-primary">Submit</button>
    </form>
    
    
    {% if name %}
        <table class="table table-striped table-hover">
        <thead>
            <tr>
            <th scope="col">Name</th>
            <th scope="col">Ability name</th>
            <th scope="col">Base Experience</th>
            <th scope="col">Sprite</th>
            </tr> 
        </thead>
        <tbody>
            {% for names in new_data %}
                <tr>
                    <th scope="row">{{data.name}}</th>
                    <td>{{data.ability_name}}</td>
                    <td>{{data.base_experience}}</td>
                    <td>{{data.sprite}}</td>      
                    
                </tr>
            {% endfor %}
        </tbody>
        </table>
    {% else %}
    
    {% endif %}


{% endblock %}